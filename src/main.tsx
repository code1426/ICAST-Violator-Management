import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <div className='text-5xl text-blue-700 items-center justify-center flex h-screen bg-slate-300'>
      ICAST
      </div>
  </StrictMode>
)
